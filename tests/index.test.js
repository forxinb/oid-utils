/**
 * Tests for oid-utils
 */

const oidUtils = require('../src/index');

describe('oid-utils', () => {
  // TODO: Add tests
  
  test('should be defined', () => {
    expect(oidUtils).toBeDefined();
  });
});
